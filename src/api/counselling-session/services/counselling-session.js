'use strict';

/**
 * counselling-session service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::counselling-session.counselling-session');
