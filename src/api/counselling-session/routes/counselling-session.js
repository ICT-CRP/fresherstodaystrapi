'use strict';

/**
 * counselling-session router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::counselling-session.counselling-session');
