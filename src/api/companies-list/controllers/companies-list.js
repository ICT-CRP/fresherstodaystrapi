'use strict';

/**
 * companies-list controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::companies-list.companies-list');
