'use strict';

/**
 * platform-feature controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::platform-feature.platform-feature');
