'use strict';

/**
 * preparation-module controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::preparation-module.preparation-module');
