'use strict';

/**
 * preparation-module router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::preparation-module.preparation-module');
